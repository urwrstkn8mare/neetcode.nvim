// Written by neetcode.nvim -- do not edit; it is regenerated.
//
// NeetCode's C++ starter code has no #includes, no `using namespace std;` and
// no definition of the helper node types, because its judge supplies all three
// implicitly. That makes a perfectly valid solution look broken to a language
// server. This header is force-included through the sibling `.clangd` file, so
// clangd sees what the judge sees. It is never compiled into a run and never
// submitted -- the local runner has its own prelude, and NeetCode gets your
// file exactly as you wrote it.
//
// Helper types are NOT declared here. `Node` is an adjacency list in Clone
// Graph and a random pointer in Copy List with Random Pointer, so there is no
// one definition that fits. Each problem gets its own header alongside this
// one, carrying the definition lifted from that problem's starter comment.
#pragma once

#include <algorithm>
#include <array>
#include <bitset>
#include <climits>
#include <cmath>
#include <cstdint>
#include <cstdio>
#include <cstdlib>
#include <cstring>
#include <deque>
#include <functional>
#include <iostream>
#include <iterator>
#include <limits>
#include <list>
#include <map>
#include <numeric>
#include <queue>
#include <set>
#include <sstream>
#include <stack>
#include <string>
#include <tuple>
#include <unordered_map>
#include <unordered_set>
#include <utility>
#include <vector>

using namespace std;
